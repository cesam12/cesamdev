# API REST de Usuários

Simples API em Node.js + Express com SQLite para demonstrar endpoints de CRUD.

Como rodar:

1. `npm install`
2. `node index.js`

Endpoints:
- GET /users
- POST /users
- GET /users/:id
- PUT /users/:id
- DELETE /users/:id
